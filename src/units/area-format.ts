import { createUnitFormat } from "../unit-registry";

// tslint:disable:variable-name

/** The metric unit for area quantities ( <code>m²</code> ). */
export const SquareMeter = createUnitFormat("m²", "SI", 2, SquareFeet);
