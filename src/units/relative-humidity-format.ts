import { createUnitFormat } from "../unit-registry";

// tslint:disable:variable-name

export const HumidityFactor = createUnitFormat("r.H. factor", "SI", 2);
export const PercentHumidity = createUnitFormat("% r.H.", undefined, 1);
