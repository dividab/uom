import { createUnitFormat } from "../unit-registry";

// tslint:disable:variable-name

// Per Duration
export const OnePerHour = createUnitFormat("/h", "SI", 2);
export const OnePerSecond = createUnitFormat("/s", "SI", 2);
