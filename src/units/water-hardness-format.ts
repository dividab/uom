import { createUnitFormat } from "../format";

// tslint:disable:variable-name

// Water hardness
export const MilliGramCalciumPerLiter = createUnitFormat("mg Ca²⁺/l", "SI", 2);

export const FrenchDegree = createUnitFormat("°f", "IP", 2);
