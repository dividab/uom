import { Cubed } from "../unit-prefix";
import { Meter } from "./base-units";

// tslint:disable:variable-name

/** The metric unit for volume quantities ( <code>m³</code> ). */
export const CubicMeter = Cubed("CubicMeter", Meter);
