import { createUnitFormat } from "../format";

// tslint:disable:variable-name

/** The metric unit for acceleration quantities ( <code>m/s²</code> ). */
export const MeterPerSquareSecond = createUnitFormat("m/s²", "SI", 2);
