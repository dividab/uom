import { createUnitFormat } from "../format";

// tslint:disable:variable-name

/**
 * The unit for binary information ( <code>bit</code> ).
 */
export const Bit = createUnitFormat("bit", undefined, 0);
