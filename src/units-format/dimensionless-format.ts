import { createUnitFormat } from "../unit-format";

// tslint:disable:variable-name

export const One = createUnitFormat(" ", 2);
export const Percent = createUnitFormat("%", 2);
export const PPM = createUnitFormat("ppm", 0);
