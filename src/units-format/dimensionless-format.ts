import { createUnitFormat } from "../format";

// tslint:disable:variable-name

export const One = createUnitFormat(" ", undefined, 2);
export const Percent = createUnitFormat("%", undefined, 2);
export const PPM = createUnitFormat("ppm", undefined, 0);
