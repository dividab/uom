import { createUnitFormat } from "../unit-format";

// Thermal conductivity
export const WattPerMeterPerKelvin = createUnitFormat("W/m/°K", 2);
export const BtuPerHourPerFeetPerFahrenheit = createUnitFormat(
  "BTU/h/ft/°F",
  2
);
